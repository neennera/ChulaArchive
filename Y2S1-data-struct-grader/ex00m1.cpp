#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ft first
#define sd second

int N, M;
int a[1000000];
priority_queue<int> c;

void getData()
{
    cin >> N >> M;
    for (int i = 0; i < N; i++)
    {
        cin >> a[i];
        c.push()
    }
}

int main()
{
    ios_base::sync_with_stdio(0), cin.tie(0);
    getData();
    return 0;
}